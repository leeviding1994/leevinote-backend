server:
  port: 8080
spring:
  application:
    name: leevinote
  datasource:
    url: jdbc:postgresql://localhost:5432/leevinote
    username: postgres
    password: ab.123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
jwt:
  secret: ${JWT_SECRET:fDUajqcNx7xdTRGEuNGcX+ZWyvKh+yX0fUl0QKaG2+A=}  # 建议使用强密钥，可通过UUID生成
  expiration: 86400000 # token过期时间，单位毫秒（此处为1天）

